import React from 'react'

import './normalize.css'
import './layout.css'

const Layout = ({ children }) => (
  <div class="container">
    <img src="images/logo.svg" alt="Beyond the Fly" width={300} />
  </div>
)

export default Layout
